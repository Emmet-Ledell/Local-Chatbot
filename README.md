Project

Chunks are 800-1000 tokens long with 100 token overlap

To run start a local instance with ollama server then in another terminal run **npm run start** to interact with the chatbot via CLI
type /close to close